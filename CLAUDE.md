# プロジェクトルール

## テスト

機能の追加・変更を行う際は、必ず以下のテストを伴わせること。

1. **単体テストを作成すること**
   - 新しいロジック(`src/lib` など)を追加・変更したら、対応する単体テストを `tests/unit/` に追加する。
   - 単体テストは古典学派(Classicist)の方針で書く:
     - 可能な限り本物のオブジェクトを使う(モック/スタブは持ち込まない)。
     - 検証するのは「状態」や「結果」。
     - テスト対象は「振る舞いの単位」(必ずしも 1 関数・1 クラスではない)。
   - 実行: `npm test`

2. **フロントエンドにも関わる部分は、E2E テストに動作確認を追加すること**
   - 画面の挙動(UI からの操作・表示)に影響する変更は、`tests/e2e/` に動作確認を追加する。
   - 本物のアプリをブラウザから操作し、結果を検証する。
   - 実行: `npm run test:e2e`

詳細な実行手順は [README.md](README.md) の「テスト」節を参照。

## PRレビュー

PRを作成・監視しているときは、以下の流れを徹底すること。

1. **CI(単体テスト・E2Eテスト)が green になったら、そのPRのコードレビューを行うこと**
   - 対象はそのPRの差分(base ブランチとの diff)。
   - 正しさのバグと、再利用性・簡潔さ・効率の観点での改善点を確認する。
2. **レビュー結果を精査し、対応の要否を判断すること**
   - 対応が必要な指摘は修正し、テスト(上記「テスト」節の方針に従う)を通してから再度pushする。
   - 対応不要と判断した指摘は、その理由をコメントなどで簡潔に記録する。
3. CIが赤のままレビューを終えたり、レビュー指摘を放置したまま「完了」と報告したりしないこと。
